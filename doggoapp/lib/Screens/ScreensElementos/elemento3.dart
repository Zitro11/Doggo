import 'package:flutter/material.dart';

class Elemento3 extends StatelessWidget {
  const Elemento3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        title: const Text("Pantalla Elemento 3"),
        backgroundColor: Colors.brown.shade100,
      ),

      body: const Center(
        child: Text('Elemento 3'),
      )













    );
  }
}