import 'package:flutter/material.dart';

class Elemento5 extends StatelessWidget {
  const Elemento5({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        title: const Text("Pantalla Elemento 5"),
        backgroundColor: Colors.brown.shade100,
      ),

      body: const Center(
        child: Text('Elemento 5'),
      )













    );
  }
}