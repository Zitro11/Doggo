import 'package:flutter/material.dart';

class Elemento2 extends StatelessWidget {
  const Elemento2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        title: const Text("Pantalla Elemento 2"),
        backgroundColor: Colors.brown.shade100,
      ),

      body: const Center(
        child: Text('Elemento 2'),
      )













    );
  }
}