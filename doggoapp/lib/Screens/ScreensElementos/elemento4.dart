import 'package:flutter/material.dart';

class Elemento4 extends StatelessWidget {
  const Elemento4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        title: const Text("Pantalla Elemento 4"),
        backgroundColor: Colors.brown.shade100,
      ),

      body: const Center(
        child: Text('Elemento 4'),
      )













    );
  }
}